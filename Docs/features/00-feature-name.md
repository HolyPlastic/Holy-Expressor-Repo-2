# [Feature Name]

**Files:** `path/to/file.js` → `functionOne()`, `functionTwo()` | `path/to/hostscript.jsx` → `jsxFunction()`
**UI Section:** [Name of the UI area this feature lives in, if applicable]

[One short paragraph: what this feature does and why it exists. Include any non-obvious behavioural rules — reverse logic, conditional branches, state dependencies.]

*For [any cross-cutting system this feature relies on], see `Docs/ARCHITECTURE.md`.*

---

## [Sub-feature — e.g. 1.1 Capture]

[Describe this sub-feature's behaviour in detail. Include branching logic, edge cases, and any silent behaviours an agent needs to know about.]

> ⚠️ [Inline warning for a local gotcha — use sparingly, only for genuine traps specific to this feature.]

---

## [Sub-feature — e.g. 1.2 Recall]

[Same pattern.]

---

## Open Bugs

*When a bug is resolved: apply `~~strikethrough~~` and add a Dev Log entry noting the fix. Do not delete.*

- **[Bug name]** — [What goes wrong, under what conditions, and what's known about the root cause. Note any workarounds already in place.]

---

## Dev Log

- 1: Initial implementation.
